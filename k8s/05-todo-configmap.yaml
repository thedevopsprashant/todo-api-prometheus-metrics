apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-api-config
  namespace: todo
  labels:
    app: todo-api
data:
  SERVER_PORT: "8082"
  # Actuator — expose all endpoints (mirrors application.properties)
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "*"
